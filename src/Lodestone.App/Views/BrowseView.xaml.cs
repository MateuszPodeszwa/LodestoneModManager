using System.Windows.Controls;

namespace Lodestone.App.Views;

/// <summary>Code-behind for the Browse screen (mod catalog search). It only initializes the XAML;
/// all behaviour lives in BrowseViewModel.</summary>
public partial class BrowseView : UserControl
{
    public BrowseView() => InitializeComponent();
}

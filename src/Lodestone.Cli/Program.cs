// Lodestone headless CLI — a thin surface over the same Application/Infrastructure layers the
// WPF app uses. Fleshed out alongside the core; for now it proves the core builds & runs UI-free.

Console.WriteLine("Lodestone CLI");
Console.WriteLine("This headless surface will expose install / list / update commands over the core.");
return 0;
